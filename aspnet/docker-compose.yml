version: '3.8'

services:

  db:
    image: mysql:8.0.33
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: 4spn3t_r00t
      MYSQL_DATABASE: questions_db
      MYSQL_USER: questions_api_server
      MYSQL_PASSWORD: 4spn3t
    ports:
      - "3306:3306"

  api:
    build:
      context: ./
      target: aspnet-questions-backend
    command: sh -c "dotnet build --configuration Debug && dotnet run --configuration Debug"
    environment:
      DB_HOST: 192.168.1.21
      DB_PORT: 3306
      MYSQL_DATABASE: questions_db
      MYSQL_USER: questions_api_server
      MYSQL_PASSWORD: 4spn3t
      PORT: 8080
    volumes:
      - .:/home/app/aspnet-questions-backend/
    ports:
      - "8080:8080"
    depends_on:
      - db